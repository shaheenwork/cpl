<?xml version="1.0" encoding="utf-8"?>
<!--
  Permissions required by the Firebase SDKs. Declared here rather than in :app so the
  module is self-contained: any consumer of :core:firebase gets what it needs, and lint
  can verify it (BUILD_PROMPT.md §4.2).

  POST_NOTIFICATIONS is deliberately NOT here. It belongs to :core:notifications in
  Phase 17, together with the runtime permission request and the privacy settings that
  govern it (§16.2).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

</manifest>
