// Typed, allowlisted analytics facade. Direct FirebaseAnalytics use is banned elsewhere (§17.1).
plugins {
    id("afterhours.android.library")
}

android {
    namespace = "com.shnapps.couple.core.analytics"
}
