// Mutual discovery: the things both partners chose, revealed one at a time
// (BUILD_PROMPT.md sections 5.2, 5.3 and 14.7).
plugins {
    id("afterhours.android.feature")
}

android {
    namespace = "com.shnapps.couple.feature.discovery"
}
