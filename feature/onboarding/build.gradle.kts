// Splash routing, the 18+ age gate and the welcome beats (BUILD_PROMPT.md §14.1, §3.1).
plugins {
    id("afterhours.android.feature")
}

android {
    namespace = "com.shnapps.couple.feature.onboarding"
}

