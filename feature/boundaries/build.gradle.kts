// Private boundaries and the user's own content level: how far things can go, and what
// is never on the table (BUILD_PROMPT.md sections 3.2 and 14.9).
plugins {
    id("afterhours.android.feature")
}

android {
    namespace = "com.shnapps.couple.feature.boundaries"
}
