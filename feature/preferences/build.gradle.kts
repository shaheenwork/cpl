// Private preference discovery: the taxonomy of curiosities, one card at a time, answered
// for the user's eyes only (BUILD_PROMPT.md §9.5, §14.7).
plugins {
    id("afterhours.android.feature")
}

android {
    namespace = "com.shnapps.couple.feature.preferences"
}
