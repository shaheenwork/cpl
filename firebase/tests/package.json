{
  "name": "afterhours-rules-tests",
  "private": true,
  "type": "module",
  "description": "Security-rules tests. Every privacy claim in BUILD_PROMPT.md section 5 must be provable here.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
